name: Publish APT repo
on:
  release: { types: [published] }
  workflow_dispatch:

permissions: { contents: write }

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout gh-pages branch
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          fetch-depth: 0

      - name: Install tools
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y dpkg-dev apt-utils jq xz-utils
          sudo snap install gh || true

      - name: Ensure layout (clean stale dists) + disable Jekyll
        run: |
          set -euo pipefail
          # start from a clean dists tree so Release paths are correct
          rm -rf dists
          mkdir -p dists/stable/main/binary-amd64
          mkdir -p pool/main/d/demodebcli
          touch .nojekyll

      - name: Download latest release .deb
        env: { GH_TOKEN: ${{ github.token }} }
        run: |
          set -euo pipefail
          mkdir -p incoming
          gh release download --repo "$GITHUB_REPOSITORY" \
            --pattern 'demodebcli_*_amd64.deb' --dir incoming --clobber
          cp incoming/demodebcli_*_amd64.deb pool/main/d/demodebcli/
          ls -l pool/main/d/demodebcli/

      - name: Build Packages (under main/binary-amd64)
        run: |
          set -euo pipefail
          dpkg-scanpackages -m pool /dev/null > dists/stable/main/binary-amd64/Packages
          gzip -f -9 dists/stable/main/binary-amd64/Packages
          xz   -f    dists/stable/main/binary-amd64/Packages

      - name: Build Release (unsigned; ok with [trusted=yes])
        run: |
          set -euo pipefail
          cat > release.conf <<'EOF'
          APT::FTPArchive::Release::Suite "stable";
          APT::FTPArchive::Release::Codename "stable";
          APT::FTPArchive::Release::Components "main";
          APT::FTPArchive::Release::Architectures "amd64";
          EOF
          # run from repo root; target dists/stable so entries i
